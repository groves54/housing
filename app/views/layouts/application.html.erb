<!DOCTYPE html>
<html>
  <head>
    <title>MU Housing</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="topnav" id="myTopnav">
    <a href="/index" class="active"><%= image_tag("muhousing.png", :size => "160x60") %></a>
    <a href="/student">Student</a>

    <a href="javascript:void(0);" class="icon" onclick="myFunction()">&#9776;</a>
  </div>



  <main role="main">
    <div class='container'>
      <%= yield %>
    </div>
  </main>
  </body>
  <footer>
    <p>&copy;2018 MU Housing by Bryan Groves and Michael Bryant </p>
  </footer>
  </html>
  <script>
      function myFunction() {
          var x = document.getElementById("myTopnav");
          if (x.className === "topnav") {
              x.className += " responsive";
          } else {
              x.className = "topnav";
          }
      }
  </script>

  <style>

    @import 'https://fonts.googleapis.com/css?family=Raleway';
    .topnav {
      background-color: #05ff00;
      overflow: hidden;
      box-shadow: 0 4px 6px 0 rgba(0,0,0,0.5);
      font-family: Raleway;
      width:100%;
    }

    footer{
      z-index: 1;
      position:absolute;
      bottom:0;
      width:100%;
      height:60px;   /* Height of the footer */
      background: #06ff08;
      color: rgb(0, 0, 0);
      text-align: center;
    }
    /* Style the links inside the navigation bar */
    .topnav .active {
      float: left;
      display: block;
      color: rgb(0, 0, 0);
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 18px;
    }

    .topnav a, .topnav div a {
      float: right;
      display: block;
      color: rgb(0, 0, 0);
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 18px;
      padding-bottom: 36px;
      padding-top: 36px;
    }

    /* Change the color of links on hover */
    .topnav a:hover, .topnav div a:hover {
      background: -webkit-linear-gradient(rgba(255,255,255,0), rgb(3, 136, 0)); /* For Safari 5.1 to 6.0 */
      background: -o-linear-gradient(rgba(255,255,255,0), rgb(3, 136, 0)); /* For Opera 11.1 to 12.0 */
      background: -moz-linear-gradient(rgba(255,255,255,0), rgb(3, 136, 0)); /* For Firefox 3.6 to 15 */
      background: linear-gradient(rgba(255,255,255,0), rgb(3, 136, 0));
      color: black;
    }

    /* Add an active class to highlight the current page */
    .active {
      background-color: #05ff00;
      color: white;
    }

    /* Hide the link that should open and close the topnav on small screens */
    .topnav .icon {
      display: none;
    }

  </style>

